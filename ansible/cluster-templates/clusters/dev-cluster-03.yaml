# dev-cluster-03: CAPI Docker+RKE2 workload cluster.
# The rancher-auto-import label causes Turtles to automatically register
# this cluster with Rancher Manager once it becomes provisioned.
apiVersion: cluster.x-k8s.io/v1beta2
kind: Cluster
metadata:
  name: dev-cluster-03
  namespace: default
  labels:
    cluster-api.cattle.io/rancher-auto-import: "true"
  annotations:
    cluster-api.cattle.io/upstream-system-agent: "true"
spec:
  clusterNetwork:
    pods:
      cidrBlocks:
        - 10.30.0.0/16
    services:
      cidrBlocks:
        - 10.31.0.0/16
    serviceDomain: cluster.local
  topology:
    classRef:
      name: docker-rke2
    version: v1.34.6+rke2r3
    controlPlane:
      replicas: 1
    workers:
      machineDeployments:
        - class: default-worker
          name: md-0
          replicas: 2
    variables:
      - name: dockerImage
        value: kindest/node:v1.34.6
